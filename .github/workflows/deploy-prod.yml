name: CD — Deploy to Production

on:
  push:
    branches: [production]

jobs:
  deploy-backend-prod:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout deploy repo
        uses: actions/checkout@v4

      - name: Read image tag
        id: tag
        run: echo "IMAGE_TAG=$(cat manifests/image-tag.txt)" >> $GITHUB_OUTPUT

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy backend to Container Apps
        run: |
          az containerapp update \
            --name inttime-backend-prod \
            --resource-group interactive-timeline-rg \
            --image ghcr.io/natestarner/inttime-backend:${{ steps.tag.outputs.IMAGE_TAG }} \
            --set-env-vars \
              "SPRING_DATASOURCE_URL=${{ secrets.SQL_CONNECTION_STRING_PROD }}" \
              "SPRING_PROFILES_ACTIVE=production"

      - name: Configure GHCR pull credentials
        run: |
          az containerapp registry set \
            --name inttime-backend-prod \
            --resource-group interactive-timeline-rg \
            --server ghcr.io \
            --username natestarner \
            --password ${{ secrets.GHCR_TOKEN }}

  deploy-frontend-prod:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout deploy repo
        uses: actions/checkout@v4

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.SWA_DEPLOYMENT_TOKEN_PROD }}
          action: "upload"
          app_location: "frontend-build"
          skip_app_build: true